# Brief Oak

Replaces Oak's intro speech text with snappy one-liners to get you through it faster, without removing any steps.

![Screenshot of the mod in action.](/.github/assets/thumbnail.png)

## Layout

- `manifest.json` - identity, version range, load order
- `main.lua` - the entry chunk; receives the `mod` object
